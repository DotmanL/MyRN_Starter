export enum ThemeType {
  Auto = "Auto",
  Dark = "Dark",
  Light = "Light"
}

export type ThemeColours = {
  surface: string;
  onSurface: string;
  primary: string;
  borderColor: string;
  red: string;
  yellow: string;
};
